<%= simple_form_for :editorship, :url => editorships_path(:hotspot_id => @hotspot.id) do |f| %>
  <%= f.input :user_email, :label => false, :placeholder => 'Invite By Email' %>
  <%= f.button :submit, 'Add' %>
<% end %>

